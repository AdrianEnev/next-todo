const clearLocalStorage = () => {
    // Clear all local storage items
    localStorage.clear();
    
    // Optionally, you can log a message to confirm that local storage has been cleared
    console.log('Local storage cleared');
}
